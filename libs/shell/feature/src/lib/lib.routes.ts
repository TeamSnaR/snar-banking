import { Route } from '@angular/router';
import { ShellFeatureComponent } from './shell-feature.component';

export const shellFeatureRoutes: Route[] = [
  { path: '', component: ShellFeatureComponent },
];
